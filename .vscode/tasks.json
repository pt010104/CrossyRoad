{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "make",
            "args": [
                "-f",
                "${workspaceFolder}/makefile",
                "-o",
                "CrossyRoad.exe",
                "-mwindows" //Target subwindows
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ]
        }
    ]
}